/*SERIA ACTUALIZAR*/
create or replace PROCEDURE DEL_ESTADO 
(IDESTADO in ESTADO.id_estado%TYPE )AS
BEGIN
DELETE FROM VENTAS.ESTADO WHERE ID_ESTADO=IDESTADO; 
END DEL_ESTADO;

create or replace PROCEDURE DEL_ROL
(IDROL IN ROL.id_rol%TYPE)AS
BEGIN 
DELETE FROM VENTAS.ROL WHERE ID_ROL=IDROL; 
END DEL_ROL;

create or replace PROCEDURE DEL_DEPARTAMENTO
(IDDEPARTAMENTO IN DEPARTAMENTO.id_departamento%TYPE)AS
BEGIN
DELETE FROM  VENTAS.DEPARTAMENTO  WHERE ID_DEPARTAMENTO=IDDEPARTAMENTO;
END DEL_DEPARTAMENTO;

create or replace PROCEDURE DEL_MUNICIPIOS
(IDMUNICIPIO IN MUNICIPIOS.id_municipios%TYPE)AS
BEGIN 
DELETE FROM VENTAS.MUNICIPIOS WHERE ID_MUNICIPIOS=IDMUNICIPIO;
END DEL_MUNICIPIOS;

create or replace PROCEDURE DEL_CATEGORIA
(IDCATEGORIA IN CATEGORIA.id_categoria%TYPE)AS
BEGIN
DELETE FROM VENTAS.CATEGORIA WHERE ID_CATEGORIA=IDCATEGORIA;
END DEL_CATEGORIA;

create or replace PROCEDURE DEL_DIRECCIONES
(IDDIRECCION IN DIRECCIONES.id_direccion%TYPE)AS
BEGIN 
DELETE FROM VENTAS.DIRECCIONES WHERE ID_DIRECCION=IDDIRECCION ;
END DEL_DIRECCIONES;

create or replace PROCEDURE DEL_PROVEEDOR
(IDPROVEEDOR IN PROVEEDOR.id_proveedor %TYPE)AS
BEGIN 
DELETE FROM  VENTAS.PROVEEDOR  WHERE ID_PROVEEDOR=IDPROVEEDOR;
END DEL_PROVEEDOR;

create or replace PROCEDURE DEL_PERSONA
(IDPERSONA IN PERSONA.id_persona%TYPE)AS
BEGIN
DELETE FROM  VENTAS.PERSONA WHERE ID_PERSONA=IDPERSONA; 
END DEL_PERSONA;

create or replace PROCEDURE DEL_USUARIOS
(IDUSUARIO IN USUARIOS.id_usuario%TYPE)AS
BEGIN
DELETE FROM VENTAS.USUARIOS WHERE ID_USUARIO=IDUSUARIO;
END DEL_USUARIOS;

create or replace PROCEDURE DEL_ARTICULOS
(IDARTICULO IN ARTICULOS.id_articulo%TYPE)AS
BEGIN
DELETE FROM VENTAS.ARTICULOS WHERE ID_ARTICULO=IDARTICULO;
END DEL_ARTICULOS;

create or replace PROCEDURE DEL_TIPOPAGO
(IDTIPOPAGO IN TIPO_PAGO.id_tipo_pago%TYPE)AS
BEGIN
DELETE FROM VENTAS.TIPO_PAGO WHERE ID_TIPO_PAGO=IDTIPOPAGO;
END DEL_TIPOPAGO;

create or replace PROCEDURE DEL_TIPODOCUEMNTO
(IDDOCUMENTO IN TIPO_DOCUMENTO.id_documento%TYPE)AS
BEGIN
DELETE FROM  VENTAS.TIPO_DOCUMENTO WHERE ID_DOCUMENTO=IDDOCUMENTO;
END DEL_TIPODOCUEMNTO;

create or replace PROCEDURE DEL_PEDIDOS
(IDPEDIDO IN PEDIDO.id_pedido%TYPE)AS
BEGIN
DELETE FROM VENTAS.PEDIDO WHERE ID_PEDIDO=IDPEDIDO;
END DEL_PEDIDOS;

create or replace PROCEDURE DEL_DETALLEPEDIDO
(IDPEDIDODETALLE IN DETALLE_PEDIDO.id_pedido_detalle%TYPE)AS
BEGIN 
DELETE FROM  VENTAS.DETALLE_PEDIDO WHERE ID_PEDIDO_DETALLE = IDPEDIDODETALLE;
END DEL_DETALLEPEDIDO;
